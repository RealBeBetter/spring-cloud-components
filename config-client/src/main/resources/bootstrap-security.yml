server:
  port: 9002
spring:
  application:
    name: config-client
  cloud:
    config:
      # 启用配置后缀名称
      profile: dev
      # 分支名称
      label: dev
      # 配置中心地址
      uri: http://localhost:8905
      # 配置文件名称
      name: config
      username: Real
      password: 123456